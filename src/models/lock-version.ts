/**
 * Represents the HTTP API model for lock version details array.
 */
export interface LockVersion {

    /**
     * Gets or sets the requested locks.
     */
    showAdminKbpwdFlag: string;

     /**
     * Gets or sets the requested locks.
     */
    groupId: string;

      /**
     * Gets or sets the requested locks.
     */
    protocolVersion: string;

     /**
     * Gets or sets the requested locks.
     */
    protocolType: string;

      /**
     * Gets or sets the requested locks.
     */
    orgId: string;

     /**
     * Gets or sets the requested locks.
     */
    logoUrl: string;

      /**
     * Gets or sets the requested locks.
     */
    scene: string;
}
