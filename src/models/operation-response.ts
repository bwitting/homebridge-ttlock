
import { Operation } from './operation';

/**
 * Represents the HTTP API model for a response with operation data.
 */
export interface OperationResponse {

    /**
     * Gets or sets the operation data.
     */
    result: Operation;
}
